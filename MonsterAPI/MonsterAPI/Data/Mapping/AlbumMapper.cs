using System;
using MonsterAPI.Model.Model_EF;
//using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace MonsterAPI.Data.Mapping
{
    //public class AlbumMapper : IEntityTypeConfiguration<Album>
    //{

    //    public void Configure(EntityTypeBuilder<Atelier_Dag_Persoon> builder)
    //    {
    //        builder.HasKey(adp => adp.ID);

    //        builder
    //            .HasOne(adp => adp.Persoon)
    //            .WithMany()
    //            .IsRequired(true)
    //            .OnDelete(DeleteBehavior.Cascade);

    //        builder
    //            .HasOne(adp => adp.AD)
    //            .WithMany(ad => ad.ADP)
    //            .IsRequired(true)
    //            .OnDelete(DeleteBehavior.Cascade);
    //    }
    //}
}
